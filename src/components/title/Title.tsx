export const Title = ({ title }: { title: string }) => {
  return (
    <h2 className="text-primary mb-[25px] text-center text-2xl font-bold capitalize lg:mb-[30px] lg:text-4xl">
      {title}
    </h2>
  );
};
